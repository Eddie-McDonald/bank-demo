server.port=8081

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:bankdemo}
spring.datasource.username=${DB_USER:bankdemo}
spring.datasource.password=${DB_PASSWORD:bankdemo}
spring.datasource.driver-class-name=org.postgresql.Driver
